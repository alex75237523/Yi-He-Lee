# 建置與驗證結果

## 2026-07-12 進場價與現價雙重均價條件（分支 `fix/dual-price-moving-average-condition`）

在具備 .NET SDK（`9.0.200-preview`）、Windows 11 的環境下實際執行：

```text
dotnet restore YiHeLee.sln                                → 成功（僅既有 SQLitePCLRaw.lib.e_sqlite3 2.1.6 NU1903 高風險套件警告，非本次異動引入，非阻斷）
dotnet build YiHeLee.sln -c Debug                          → 建置成功，0 錯誤
dotnet build YiHeLee.sln -c Release                        → 建置成功，0 錯誤
dotnet test  YiHeLee.sln -c Release --filter Category!=Integration
                                                             → 已通過 266／266，0 失敗
dotnet test  YiHeLee.sln -c Release --filter Category=Integration
                                                             → 已通過 4／4，0 失敗（實際連線 TWSE／TPEx 官方端點）
.\scripts\publish-win-x64.ps1                               → 成功，輸出 publish\win-x64，並產生 buildinfo.json
```

自我 Code Review 重點：

- `CustomerHolding`／`StrategyAlert` 新欄位（`EntryAveragePrice`／`EntryAveragePriceIssue`／`CurrentPriceIssue`）皆以具預設值的選填參數加在既有欄位之後，逐一比對所有既有建構呼叫點（`ExcelWorkbookService`、`StrategyEvaluationService`、`SqliteYiHeLeeRepository`、全部測試檔案），確認參數順序未錯位；`HoldingStrategyResult` 因僅在 `StrategyEvaluationService.EvaluateAll` 內部建構（無外部呼叫點），重新排序後同步更新該方法內全部建構點。
- 新增／修改 SQL（`CustomerHoldingSnapshots`／`StrategyAlerts` 之 `ALTER TABLE ADD COLUMN`、`UpsertHoldingAsync`／`UpsertAlertAsync` 之 `INSERT ... ON CONFLICT`）皆使用參數化查詢，無外部輸入拼接；`StrategyAlerts` 唯一鍵重建（`RENAME` → `SchemaSql` → `INSERT ... SELECT` → `DROP`）沿用既有 `MigrateEntryAveragePriceToCurrentPriceAsync` 已驗證過的安全模式，欄位名稱皆來自程式內固定常數。
- **發現並修正一項潛在資料損毀風險**：新版 `EntryAveragePrice` 欄位名稱恰與 2026-07-09 之前的舊版歷史欄位同名；若不處理，既有 `MigrateEntryAveragePriceToCurrentPriceAsync` 會在下次啟動時誤判新欄位為舊版尚未搬移的資料，用新版數值覆蓋 `CurrentPrice`。已修正為額外檢查 `CurrentPrice` 是否已存在，只有真正尚未改名的舊格式才觸發搬移，並新增測試驗證（`SqliteYiHeLeeRepositoryMigrationTests`）。
- 確認 Excel「每日五日均價策略」七欄輸出（`ExcelWorkbookService.WriteStrategyResultsCore`／`OutputColumnCount`／`OutputRangeLastColumn`）未被修改。
- `git status --short` 確認未提交 `bin`、`obj`、`.vs`、`Logs`、`Data`、`Backups`、DLL、EXE、SQLite 正式資料庫或 Excel 客戶正式資料；diff 內容掃描未發現密碼、API Key、Token 或正式連線字串。

### 本次未能實機驗證的項目

本次執行環境無法啟動互動式 GUI 處理序，因此以下項目僅完成程式碼審閱與自動測試（含新增 `MainFormResultsFormattingTests` 對 `MainForm.Results.cs` internal 格式化方法的純邏輯驗證），未能以真實 Excel 活頁簿及 WinForms 畫面端到端實機驗證：

- 中央「符合均價條件」頁籤「進場價/平均價」「現價」欄位分開顯示、「判斷明細」欄位自動換行的實際呈現效果。
- 新增「進場價／平均價異常」頁籤的實際顯示與欄寬。
- 使用實際客戶 Excel（`親帶績效最新版0701.xlsx` 或同等最新工作簿）驗證 D 欄「進場價/平均價」、F 欄「現價」讀取正確，`#NAME?` 等錯誤值判定為現價異常而非進場價異常（反之亦然）。
- Windows 系統匣、13:35 排程觸發之互動式桌面驗收（環境限制與既有文件一致，見 `docs/09_人工驗收與完成回報.md`）。

## 2026-07-11 DDE 現價異常隔離、全部持股計算結果輸出（分支 `fix/dde-independent-calculation-pipeline`）

在具備 .NET SDK（`9.0.200-preview`）、Windows 11、已安裝 Microsoft Excel 的環境下實際執行，並於執行前先清除舊的 `bin`、`obj`、`publish` 建置輸出後重新建置：

```text
dotnet restore YiHeLee.sln                                → 成功（僅既有 SQLitePCLRaw.lib.e_sqlite3 2.1.6 NU1903 高風險套件警告，非本次異動引入，非阻斷）
dotnet build YiHeLee.sln -c Debug                          → 建置成功，0 錯誤，0 警告（本次順便修正既有 6 個 CS8601 可為 Null 警告）
dotnet build YiHeLee.sln -c Release                        → 建置成功，0 錯誤
dotnet test  YiHeLee.sln -c Release --filter Category!=Integration
                                                             → 已通過 223／223，0 失敗（含本次新增 12 個測試：DailyJobServiceTests 11 個＋SqliteYiHeLeeRepositoryMigrationTests 1 個）
dotnet test  YiHeLee.sln -c Release --filter Category=Integration
                                                             → 已通過 4／4，0 失敗（實際連線 TWSE／TPEx 官方端點）
.\scripts\publish-win-x64.ps1                               → 成功，輸出 publish\win-x64，並產生 buildinfo.json（Git Commit SHA、分支、Build 時間）
```

自我 Code Review：新增／修改 SQL（`CustomerHoldingSnapshots` 新增 `CurrentPriceIssue` 欄位之 `ALTER TABLE`、`UpsertHoldingAsync` 之 `INSERT ... ON CONFLICT`）皆使用參數化查詢或程式內固定欄位名稱，無外部輸入拼接。`git status --short` 確認未提交 `bin`、`obj`、`.vs`、`Logs`、`Data`、`Backups`、DLL、EXE、SQLite 正式資料庫或 Excel 客戶正式資料；`publish/` 已由 `.gitignore` 排除，`buildinfo.json` 隨 `publish/` 一併排除。diff 內容掃描未發現密碼、API Key、Token 或正式連線字串。

### 執行檔實際路徑確認（回應「不得啟動舊 EXE」要求）

本機掃描（`Get-ChildItem -Filter "Yi He Lee.exe" -Recurse` 於 `C:\`）找到以下既有執行檔，本次已清除 `bin`／`obj`／`publish` 後重新建置，確保後續測試啟動的是本次新版：

- `C:\codex\Yi He Lee\YiHeLee\publish\win-x64\Yi He Lee.exe`（本次 Publish 產生，正式發佈路徑）
- `C:\codex\Yi He Lee\YiHeLee\src\YiHeLee.App\bin\Debug\net8.0-windows\Yi He Lee.exe`（本次 Debug Build 產生）
- `C:\codex\Yi He Lee\YiHeLee\src\YiHeLee.App\bin\Release\net8.0-windows\Yi He Lee.exe`（本次 Release Build 產生）
- 未發現 `C:\YiHeLeeTest` 或其他部署資料夾殘留的舊版本。

### 本次未能實機驗證的項目

本次執行環境無法啟動互動式 GUI 處理序（與既有文件一致的環境限制），因此以下項目僅完成程式碼審閱與 Fake／Spy 自動測試（`DailyJobServiceTests`，以完整依賴鏈但不啟動 Excel COM／SQLite／HTTP 驗證流程順序與資料正確性），未能以真實 Excel 活頁簿端到端實機驗證：

- `ExcelWorkbookService.WriteStrategyResultsCore` 依最新規則寫入 7 欄均價前置格式（代碼、名稱、收盤價、5日均價、20日均價、60日均價、120日均價）。
- Windows 系統匣、中央通知視窗、主視窗標題列版本／Commit SHA 顯示之互動式桌面驗收。
- 使用實際客戶 Excel 完成 DDE 正常／`#N/A`／空白／正常未觸發／均線資料不足五種持股的端到端人工驗證（見 `docs/09_人工驗收與完成回報.md`）。

## 2026-07-11 修正逐檔歷史完整性、股票代碼正規化與 Excel 表格邊界（分支 `fix/per-stock-moving-average-history`）

在具備 .NET SDK（`9.0.200-preview`）、Windows 11、已安裝 Microsoft Excel 的環境下實際執行：

```text
dotnet restore YiHeLee.sln                                → 成功（僅既有 SQLitePCLRaw.lib.e_sqlite3 2.1.6 NU1903 高風險套件警告，非本次異動引入，非阻斷）
dotnet build YiHeLee.sln -c Debug                          → 建置成功，0 錯誤，11 個警告（皆為既有 CS8601 可為 Null 警告，非本次新增）
dotnet build YiHeLee.sln -c Release                        → 建置成功，0 錯誤，11 個警告
dotnet test  YiHeLee.sln -c Release --filter Category!=Integration
                                                             → 已通過 211／211，0 失敗（含本次新增 58 個測試）
dotnet test  YiHeLee.sln -c Release --filter Category=Integration
                                                             → 已通過 4／4，0 失敗（實際連線 TWSE／TPEx 官方端點）
dotnet test  YiHeLee.sln -c Release（全部，含 Integration） → 已通過 215／215，0 失敗
```

自我 Code Review：所有新增／修改 SQL 皆使用參數化查詢（`$paramName` + `AddWithValue`）；唯一以字串插值組成的 SQL 為 `ALTER TABLE StockMovingAverage ADD COLUMN {name} {definition}`，`name`／`definition` 來自程式內固定的欄位定義陣列（非外部輸入），屬既有 `MigrateStockDailyPriceColumnsAsync` 已使用的相同安全模式。`git status --short` 確認未提交 `bin`、`obj`、`.vs`、`Logs`、`Data`、`Backups`、DLL、EXE、SQLite 正式資料庫或 Excel 客戶正式資料；diff 內容掃描未發現密碼、API Key、Token 或正式連線字串。

### 本次未能實機驗證的項目

本次執行環境雖有 Windows 桌面與 Microsoft Excel（`C:\Program Files\Microsoft Office\root\Office16\EXCEL.EXE`），但未取得使用者實際客戶 Excel 檔案，因此以下項目僅完成程式碼審閱與可行範圍內的純邏輯單元測試（`ExcelWorkbookServiceHeaderDetectionTests`，直接呼叫 `internal` 方法驗證表格邊界邏輯、不啟動 Excel COM），未能以真實 Excel 活頁簿端到端實機驗證：

- `ExcelWorkbookService.WriteStrategyResultsCore` 實際寫入 14 欄新版輸出格式、合併儲存格處理、頁籤保護偵測。
- `ExcelWorkbookService.ReadHoldingsFromWorksheet` 對真實客戶頁籤的「現貨現價」表頭辨識、其他表格邊界停止、股票名稱空白略過、連續空白列停止掃描。
- 連續多日執行後，5351／8299／3691 等指定股票 MA5／MA20／MA60／MA120 逐步補齊的長期觀察。
- Windows 系統匣、中央通知視窗、13:35 排程觸發之互動式桌面驗收（環境限制與既有文件一致，見 `docs/09_人工驗收與完成回報.md`）。

## 2026-07-09 Claude Code 接手：實際 Build／Test 結果

本次在具備 .NET 8 SDK（`9.0.102`）、Windows 11 的環境下實際執行（而非僅靜態掃描）：

```text
dotnet restore .\YiHeLee.sln         → 成功（僅 SQLitePCLRaw.lib.e_sqlite3 2.1.6 NU1903 高風險套件警告，非阻斷）
dotnet build .\YiHeLee.sln -c Debug   → 建置成功，0 錯誤，7 個警告
dotnet build .\YiHeLee.sln -c Release → 建置成功，0 錯誤，14 個警告
dotnet test  .\tests\YiHeLee.Tests\YiHeLee.Tests.csproj -c Release
                                       → 已通過 14／14，0 失敗
dotnet publish（win-x64 self-contained）→ 成功，輸出 publish\win-x64
```

修正的根因與檔案見 `docs/05_異動紀錄.md`「2026-07-09（Claude Code 接手：Build 修復與可攜式路徑）」。

### 環境限制（本次仍未能實機驗證的項目）

本次執行環境雖為真實 Windows 主機且已安裝 Microsoft Excel（`C:\Program Files\Microsoft Office\root\Office16\EXCEL.EXE`）、`explorer.exe` 桌面工作階段存在，但此代理程式執行身分無法啟動新的互動式 GUI 處理序（`Start-Process` 回報「Access is denied」，直接執行 `.exe` 回報「Permission denied」），因此以下項目**仍只完成程式碼審閱與自動測試，未能實際啟動程式驗證**：

- Windows 系統匣圖示、選單與中央通知視窗實際顯示。
- Excel Interop 連線已開啟活頁簿、忙碌／唯讀／受保護檢視／頁籤保護等防呆情境。
- Playwright／Edge 對鉅亨網兩頁面、兩市場的實際擷取與當日日期驗證。
- 13:35 排程實際觸發與可攜式資料夾搬移後的啟動測試。

已將發佈結果複製至 `C:\YiHeLeeTest`（自我測試用，不在專案目錄內，未提交 Git），供使用者在能操作桌面的工作階段下直接執行 `Yi He Lee.exe` 完成 `docs/09_人工驗收與完成回報.md` 所列的人工驗收項目。

## 本次已完成

- 以 Tree-sitter C# Parser 掃描所有 `.cs`，未發現語法節點錯誤。
- 解析所有 `.csproj` 與 `Directory.Build.props` XML，格式正確。
- 解析 `settings.example.json`，JSON 格式正確。
- 以 SQLite 執行 `database/schema.sql`，六張主要資料表及索引建立成功。
- 以使用者提供的 Excel 抽查：
  - 客戶頁籤存在沒有「自持股」文字的有效持股表。
  - 同一頁籤下方存在包含「出場價」的已出場表，程式已使用表頭邊界停止掃描。
  - `黃元琪-A` 範例的綠色股票列使用 `#92D050`，已列為預設排除色。
- 建立策略、色彩／文字排除、設定固定規則、鉅亨 Parser 自動測試原始碼。

## 尚未能在目前產生環境執行

目前容器沒有安裝 .NET SDK、Windows、Microsoft Excel 桌面版及 Office COM，因此無法在此環境實際執行：

```text
dotnet restore
dotnet build
dotnet test
Excel Interop 實機操作
Windows 系統匣與 UAC 實機操作
Playwright Edge 實機爬取
```

請在 Windows 開發機執行 `scripts/build.ps1`，並依 `docs/04_測試與驗收.md` 完成人工驗收。任何 Build 或測試錯誤都應先修正後再部署正式使用。
