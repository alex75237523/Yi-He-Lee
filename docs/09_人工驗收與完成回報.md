# 人工驗收與完成回報

## -1. 進場價高於 MA20 且現價低於 MA5 才通知（2026-07-19 正式規則，待人工驗收，最高優先）

> 2026-07-19 正式推翻舊「MA5／MA20／MA120 任一均價與進場價/平均價或現價比較（OR）」規則。唯一通知條件為複合條件
> `進場價/平均價 > MA20 AND 現價 < MA5`（必須兩項同時成立；嚴格大於／小於，相等不觸發）。MA60／MA120 只保存與顯示、不參與觸發。

使用實際客戶 Excel（`親帶績效最新版0701.xlsx` 或同等最新工作簿），以唯讀或備份副本驗證，**禁止把客戶 Excel 提交到 Git**。至少抽查以下客戶頁籤：王保仁-A、黃元琪-A、林佳玫-A。

### 邏輯案例（可用測試值或實機值對照，結果須完全一致）

| 案例 | 進場價/平均價 | MA20 | 現價 | MA5 | 預期結果 |
| --- | --- | --- | --- | --- | --- |
| 一 | 120 | 115 | 98 | 100 | 觸發 |
| 二 | 110 | 115 | 98 | 100 | 不觸發（進場價未高於 MA20，第一條不成立） |
| 三 | 120 | 115 | 102 | 100 | 不觸發（現價未低於 MA5，第二條不成立） |
| 四 | 115 | 115 | 98 | 100 | 不觸發（進場價必須嚴格大於 MA20） |
| 五 | 120 | 115 | 100 | 100 | 不觸發（現價必須嚴格小於 MA5） |

### 實機驗收重點

- D 欄「進場價/平均價」讀入正確；F 欄「現價」讀入正確；兩者為獨立欄位，畫面上不得合併顯示。
- 準備涵蓋下列情況並實際執行一次：複合條件成立（觸發）；只有第一條成立、只有第二條成立、兩條都不成立（皆不觸發）；`進場價/平均價 == MA20`、`現價 == MA5` 邊界（皆不觸發）；MA5 或 MA20 缺少（顯示無法判斷、缺少哪一條）；進場價/平均價無效（列入「進場價／平均價異常」，原因不得寫成 DDE 異常）；現價 `#N/A`（列入「現價異常」，進場價/平均價保留）；兩者同時無效（兩個異常頁籤都要看得到這筆）。
- 盤中 DDE 更新後會重新判斷；複合條件首次成立只跳一筆通知；持續成立不重複跳；任一子條件解除後再次成立可再次跳；手動「立即盤中判斷」與自動排程結果一致。
- 系統匣通知內容正確（新觸發筆數）；中央「符合通知條件」頁籤「進場價/平均價」與「現價」為兩個獨立欄位；「判斷明細」逐行列出「進場價/平均價 X > MA20 Y」「現價 X < MA5 Y」，不顯示未成立條件、不列 MA120。
- 畫面上方摘要分開顯示「符合通知條件（進場價/平均價 > MA20 且 現價 < MA5）」「均價資料異常」「進場價/平均價異常」「現價異常」「無法判斷」各自筆數。
- `13:35` 收盤更新未被破壞；已出場表格（含「出場價」欄）未被納入；不會把投入資金、損益或其他數值誤認為股票價格。
- Excel「每日五日均價策略」頁籤仍只有 7 欄（代碼、名稱、收盤價、5日均價、20日均價、60日均價、120日均價），未因本次異動新增客戶、進場價/平均價、現價或 DDE 診斷欄位。

## 0. DDE 現價異常隔離與全部持股計算結果（2026-07-11 新增，待人工驗收）

使用實際客戶 Excel，準備至少以下五種持股並實際執行一次：

1. DDE 現價正常的股票。
2. DDE 現價為 `#N/A` 的股票（看盤軟體未開啟或未連線）。
3. DDE 現價儲存格為空白的股票。
4. 現價正常但未觸發任一均線條件的股票。
5. 均線資料不足（近期上市或尚未回補完成 120 日）的股票。

驗收重點：

- DDE 無效的股票仍完成官方收盤價寫入、歷史回補、MA5／MA20／MA60／MA120 計算（若歷史已足夠）、鉅亨交叉驗證，Excel 仍有該股票完整一列；只有「整體結果」欄位標示「現價無效，暫時無法判斷」。
- 未觸發的正常持股仍出現在 Excel 輸出，不會因為沒有觸發就消失；「整體結果」顯示「未觸發」。
- 均線資料不足的股票「均線計算狀態」「缺少原因」有具體說明，不得顯示空白或「均線計算失敗」。
- Excel「每日五日均價策略」頁籤只保留 7 欄：代碼、名稱、收盤價、5日均價、20日均價、60日均價、120日均價；客戶、現價、DDE、觸發條件與診斷資訊只出現在中央結果視窗。
- 主視窗標題列顯示的版本與 Commit SHA，與 Log 中記錄的一致，且確認本次啟動的是最新 Publish（而非舊資料夾中的舊 EXE）。

## A. 可攜式測試

1. 發布到 `D:\YiHeLeeTest`。
2. 執行後確認自動建立：
   - `settings.json`
   - `Data\yi-he-lee.db`
   - `Logs`
   - `Backups`
3. 關閉程式，整個資料夾複製到另一個可寫路徑。
4. 再次啟動，確認設定與歷史資料仍可讀取。

## B. 排程測試

- 手動調整測試時間，不要修改正式 13:35 固定預設。
- 確認台北日期與 UTC 跨日時仍以台北日期為準。
- 同時按「立即執行」不得啟動兩個 Job。
- 網站日期為昨日時不得寫入當日正式資料。

## C. 爬蟲測試

- 多頭集中。
- 多頭店頭。
- 空頭集中。
- 空頭店頭。
- 驗證完整清單筆數。
- 驗證頁面日期。
- 驗證空清單與解析失敗能區分。
- 驗證網站結構改變時明確失敗。

## D. Excel 測試

1. Excel 未開啟。
2. 活頁簿未開啟。
3. 使用者正在編輯儲存格。
4. Excel 有 Modal 對話框。
5. 活頁簿唯讀。
6. 受保護檢視。
7. 輸出頁籤受保護。
8. 輸出頁籤不存在。
9. 使用者執行中關閉 Excel。
10. 程式與 Excel 權限不同。
11. 寫入後驗證失敗。
12. 儲存失敗。
13. 同時開啟兩個以上 Excel 執行個體，指定活頁簿不在第一個執行個體。
14. Excel 已開啟同名但不同資料夾的副本，確認程式拒絕只憑檔名連接。
15. 活頁簿剛開啟即按立即執行，確認會依 Excel 短暫重試設定重新連接。
16. 查看 Log 的「Excel 活頁簿連線診斷」，確認 ROT、Excel 視窗、NativeOM、已偵測路徑與探測錯誤都有紀錄。

每種情況都要確認：

- 程式不崩潰。
- Log 有原因。
- Job 有狀態。
- 中央視窗顯示原因。
- 是否重試符合規則。

## E. 頁籤解析測試

- 有「自持股」標題。
- 沒有「自持股」標題。
- 代號 `0050`。
- 代號 `00631L`。
- 代號 `00982A`。
- 下方有已出場表。
- 排除色列。
- 排除文字列。
- 正常黃色／其他顏色持股不可誤排除。
- `總表` 與輸出頁籤不可掃描。
- 2026-07-11 新增：表頭為「現貨現價」（而非「現價」）的持股表仍可正確辨識。
- 2026-07-11 新增：持股表格下方緊接「日期／權益數／保證金／小計」等彙總表格時，彙總表格內容（例如 8 位數金額）不得被誤判為持股延伸掃描。
- 2026-07-11 新增：股票名稱空白的列不得建立持股；連續空白列應停止該區塊掃描。
- 2026-07-11 新增：權益數、保證金等 7～8 位數金額不得出現在策略判斷或 Excel 輸出中。

## F. 策略測試

- 進場價 > MA20 且現價 < MA5：觸發。
- 只有進場價 > MA20（現價未低於 MA5）：不觸發。
- 只有現價 < MA5（進場價未高於 MA20）：不觸發。
- 兩子條件都不成立：不觸發。
- 進場價 == MA20、現價 == MA5 邊界：不觸發（嚴格大於／小於）。
- MA5 或 MA20 缺少：不觸發、明確顯示無法判斷缺少哪一條。
- MA60／MA120 任何數值：不影響結果。
- 缺少技術資料。
- 同一股票出現在多個來源。
- 同一客戶同一股票有多筆進場列。
- 2026-07-11 新增：股票代碼經官方主檔確認前導零回復後（例如 `50`→`0050`）仍可正確比對均線與市場別。
- 2026-07-11 新增：連續執行數日後，原本只有 MA5 的老股票（5351、8299、3691 等）應逐步補齊 MA20／MA60／MA120；若特定股票持續缺少，Excel「缺少原因」欄位需可具體說明（例如近期上市、資料本就有限，或回補過程發生錯誤），不得只顯示「歷史資料不足」而無其他細節可追查。
- 2026-07-11 新增：權證代碼不納入均線策略判斷，Excel 標示「非策略商品」。
- **2026-07-19 正式規則**：唯一通知條件為 `進場價/平均價 > MA20 AND 現價 < MA5`（兩項同時成立；嚴格大於／小於，相等不觸發）；舊「MA5／MA20／MA120 任一均價 >= 進場價/平均價或現價（OR）」規則已作廢。
- 進場價/平均價或現價其中之一無效時，不得觸發，且必須各自產生對應的異常清單。
- 兩者同時無效時，「進場價／平均價異常」與「現價異常」兩個清單都要看得到這筆持股。
- MA5 或 MA20 缺少時不得觸發，明確顯示無法判斷並說明缺少哪一條，不得用其他均線替代。

## G. 主視窗與通知測試（2026-07-09 改為常駐分頁主視窗）

- 雙擊系統匣圖示：開出常駐主視窗，預設停在「操作」頁籤，可看到「操作」「設定」「每日結果」三個頁籤。
- 系統匣選單「設定」：開啟／喚醒同一個主視窗並自動切到「設定」頁籤。
- 「設定」頁籤修改後按「儲存設定」：不關閉視窗、顯示「設定已儲存」提示；重開程式後仍生效。
- 「設定」頁籤驗證失敗（如必填欄位空白）：顯示錯誤訊息，不清空已輸入內容、不切換頁籤。
- 「操作」頁籤「立即執行」按鈕：執行中會 disable，與系統匣選單「立即執行」共用同一個忙碌判斷（同時觸發不得啟動兩個 Job）。
- 點選「立即執行盤中判斷」後，操作頁必須立刻顯示進度條，並逐步顯示目前正在執行的步驟（解析基準、讀取已保存均價、讀取 Excel、比對條件、更新通知去重等）。
- 手動盤中判斷完成後，即使沒有任何「新通知」，只要目前仍有符合均價條件、進場價/平均價異常、現價異常或無法判斷清單，就必須切到「每日結果」頁顯示完整結果。
- 自動 30 秒盤中判斷沒有新通知時，不應每 30 秒跳出或搶焦點；但若使用者已停在「每日結果」頁，內容應持續更新。
- 執行完成或失敗時，主視窗自動跳到前景並切換到「每日結果」頁籤（不再另開獨立視窗）。
- 「每日結果」頁籤：一次顯示完整清單，可捲動、可複製；成功時可切換「符合通知條件」／「無法判斷」兩個內層頁籤；失敗時可看到狀態、結果、錯誤訊息與「開啟 Log 資料夾」按鈕。
- 「每日結果」頁籤「立即重新執行」／成功「開啟 Excel」／失敗「前往設定」按鈕皆可正常運作。
- 關閉主視窗（按 X）後，程式仍留在系統匣；再次雙擊可重新叫出，且「每日結果」頁籤內容維持上次執行結果。
- 設定頁「程式圖示」留空時，主視窗、歷史收盤價視窗與右下角系統匣顯示 V2.0 內建圖示；指定有效 `.ico` 或圖片後儲存，系統匣與主視窗立即更新；指定失效路徑時退回內建圖示且主流程不受影響。
- 結束程式（系統匣選單「結束」）時，主視窗確實一併關閉，不留殘留視窗。

## H. 歷史收盤價與抓取進度測試（2026-07-09 新增）

- 系統匣選單「歷史收盤價」可正常開啟畫面。
- 交易日數預設5日，可切換 10/20/60/120/自訂（自訂範圍 1～250）。
- 「立即回補」按下後立即顯示批次進度，不需等待整批完成才有畫面回應。
- 上市／上櫃／全部市場範圍切換後，工作數量正確（N 個交易日 × 對應市場數）。
- 執行中按下「取消抓取」：批次與尚未開始的工作變為「已取消」，已完成工作不受影響。
- 重新開啟畫面（或重啟程式）後，仍可看到上一批進度（由資料庫回復）。
- 重複執行相同日期範圍的回補：不產生重複的 `StockDailyPrice` 資料列。
- 查詢結果分頁正常，切換市場別／關鍵字／日期區間篩選正確；MA5 有資料時正確顯示，MA20／60／120 資料不足時顯示「資料不足」而非0。
- 「與鉅亨網交叉驗證」按鈕：正常顯示相符／差異／資料不足／不適用／日期不符統計，且不覆蓋或影響已查詢到的官方歷史收盤價。
- 模擬網路中斷或官方端點回應 5xx／429：確認工作依指數退避重試且不超過設定次數，最終標記失敗且訊息清楚。

## 完成回報格式

```text
完成項目：
修改檔案：
資料庫影響：
排程與日期驗證：
擷取結果與筆數：
Excel 防呆與重試：
通知與系統匣：
架構檢查：
文件更新：
Debug Build：
Release Build：
Test：
Publish：
實機驗收：
尚未驗證項目：
風險提醒：
下一步建議：
```
